USAGE INSTRUCTION
#################
Please build the gemm application present in the directory before you run the validation script:

$ cd ../sw/sample/
$ make prefix=<prefix_path>

OPAE requires root access while running applications. When running validation scripts, please do sudo 

For example:

	$ sudo python validate.py -g <mode> -i <input_file>